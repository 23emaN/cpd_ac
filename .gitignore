/vendor/
/node_modules/
.history/

# Ignore the .env file so it doesn't get pushed to version control
.env
.env.backup
.env.production

# Customer Drive uploaded files — never commit actual customer documents
/storage/customer-files/*
!/storage/customer-files/index.php
